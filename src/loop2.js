var N = 4, iter=0;  
while (N % 2 == 0) {
    console.log("N= %d " + "iter = %d", N, iter);
    //exibir(linhaTabela)
    if (N > 20) break;
	console.log("info ");
      N += 2; iter++;
} 
console.log("N= %d " + "iter = %d", N, iter);

